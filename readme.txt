מסך מחשב יציאת DisplayPort
osascript -e 'tell application "QuickTime Player" to set rate of document 1 to 0.62'
<<<<<<< HEAD
git add .
git commit -m "first commit before react"
git branch -M main
git remote add origin https://github.com/perlov3301/techwithtimnew.git
git push -u origin main
#after changing model or database
% python3 ./manage.py makemigrations
% python3 ./manage.py migrate
% python3 ./manage.py runserver
# temlates/html does not exist:
   % python manage.py collectstatic
or settings.py import os dir: os....
  solved: 
  settings.py: 'installed_apps': ...,'frontend.apps.FrontendConfig'
#frontend:
    path('', index),
    path('join', index),
    path('create', index),
QUXVAP
45mglnhqj36mw9q562dap7w7pljlvg5z
=======
#after changing model or database
% python3 ./manage.py makemigrations
% python3 ./manage.py migrate
git add .
git commit -m "first commit before react"
git branch -M main
git remote add origin https://github.com/perlov3301/techwithtimnew.git
git push -u origin 
# redux
npm install react-redux
# radio values
  the onChange event returns the value as type string.Your setState then
  triggers a rerender and assigns that value to RadioGroup. It stops working
   at this point because RadioGroup has value={"true"} or {"10"}, while the
   corresponding RadioButtons has value={true} or {10}

Client ID     545bf4332f2a4d139a44a20bd84be0e7
Client Secret 0e229b44a1b34317a6ee204165307050
#developer.spotify dashboard application edit settings "Redirect URIs":
  http://127.0.0.1:8000/spotify/redirect
  http://127.0.0.1:8000/
# "room authenticate error:" TypeError: Body is disturbed or locked
# geeksforgeeks
  # Using current time 
    ini_time_for_now = datetime.now() 
    print ("initial_date", str(ini_time_for_now)) 
# NOT NULL constraint failed: spotify_spotifytoken.refresh_token
sql ('INSERT INTO "spotify_spotifytoken" ("user", "created_at", "refresh_token", 
      ' '"access_token", "expires_in", "token_type")
          VALUES (%s, %s, %s, %s, %s, %s)')
utils.py: 31. tokens.save()
views.py: spotify_callback 47. update_or_create_user_tokens(
# import logging
       print(): display console output for a command line;
       exception: raising an exception while reporting an error at runtime event;
       logging.debug(), logging.info(): report events that occur during operation;
       warnings.warn(): notes issue that is avoidable by client app at a runtime event;
       logging.warning(): notes issue when there is nothing the client can do at runtime event;
       logging.error(), logging.exception(), logging.critical(): report an error without 
                                                                 raising an axception;
# severity of events.
  DEBUG: detailed info when diagnosing problems
  INFO: things are working as expected
  WARNING: the software is still  working but will be problem in the future;
  ERROR: software has not been able to perform some function;
  CRITICAL: program may be unable to continue running;
